/** @type {import('tailwindcss').Config} */
module.exports = {
  content: [
    "./src/**/*.{js,jsx,ts,tsx}", // 🧠 Look inside all your React files for Tailwind classes
  ],
  theme: {
    extend: {}, // 🌈 You can add custom colors, fonts, etc. later here
  },
  plugins: [], // 🧩 Extra Tailwind features (if you want later)
};
